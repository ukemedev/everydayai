- [Phase 2 input capabilities](phase2-input-capabilities.md) — vision functions, upload route, whisper, Studio toggles, Chat media UI, webhook media all wired together.
- [Whisper transcribeAudio signature](whisper-signature.md) — third arg `openaiApiKey` is optional; falls back to `OPENAI_API_KEY` env var; Buffer must be wrapped in `new Uint8Array()` for File constructor.
- [Channel guard already complete](channel-guard-status.md) — T001-T003 (channelGuard.ts, Telegram setup, WhatsApp setup with ownership + exclusivity + rate limits) were all implemented before the session; only T004 SQL migration and Phase 2 media remained.
- [Dashboard analytics endpoint](dashboard-analytics.md) — `/api/analytics` returns per-user summary (agents, conversations, messages, plan usage), 7-day dailyVolume, and recent activity. Uses `in("conversation_id", convIds)` pattern with fetched IDs first; Supabase `.in()` does not accept a PostgrestFilterBuilder subquery.
- [Hero mobile spacing fix](hero-mobile-spacing.md) — `min-h-[85vh] sm:min-h-screen` prevents full-screen viewport trap on mobile. `py-8` and `pb-10` give breathing room. Reduced `mt-*` values on mobile while keeping desktop.
- [Supabase remote DB access blocked](supabase-db-access.md) — Direct psql to Supabase from Replit is blocked; exec_sql RPC doesn't exist; Management API JWT fails. Run DDL migrations manually via Supabase dashboard SQL editor. Local postgres at DATABASE_URL works fine for dev.
- [Vitest mock chains for Supabase](vitest-supabase-mocks.md) — When mocking Supabase query chains (.select().lt().not()), simplify the implementation chain to match what the mock can return. Async terminal methods must be the last in chain; intermediate chained methods must be sync returning objects.
- [Frontend vitest setup](frontend-vitest.md) — vitest + @testing-library/react not installed in frontend; pnpm install times out. Write frontend tests as .ts files in api-server test suite using fs.readFileSync for source inspection. Never put emoji in component comments when tests check source for emoji absence.
